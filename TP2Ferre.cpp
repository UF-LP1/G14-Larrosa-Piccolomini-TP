#include "headers.h"
// Hay que incluir todas las clases

int main(void) {
	// Los fondos de la ferreteria hay que intenar hacerlos lo mas altos posibles, asi evitamos errores
	// Invocar la cantidad de productos  = 0 desde el inicio del programa
	return 0;
}
